{
  "name": "snake",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p . && esbuild out/main.js --bundle --outfile=dist/bundle.js --format=iife",
    "start": "serve -s . -l $PORT",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "serve": "^14.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
